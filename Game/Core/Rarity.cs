namespace Game
{
    /// <summary>
    /// Перечисление, представляющее тип редкости объекта относительно других (влияет на его внешний вид).
    /// </summary>
    public enum Rarity
    {
        None,
        Rare,
        Epic,
    }
}
